# client-minimal — Core dev tools only, no personal apps or preferences
# Use for client-owned machines or shared environments.
# format = 1

format = 1
modules = xcode,homebrew,shell,mise,dotfiles,git,ssh,dev-dirs
brewfiles = core,dev
mise_tools = node@22,ruby@3.3,python@3.12
