# Brewfile.apps — GUI applications

cask "ngrok"               # Tunnel for local dev
cask "graphql-playground"  # GraphQL IDE
cask "icanhazshortcut"     # Global keyboard shortcuts
cask "xcodes"              # Xcode version manager
cask "secretive"           # SSH keys in Secure Enclave
cask "gpg-suite"           # GPG GUI tools
