# ~/.ssh/config — Managed by mbp
# Per-client SSH entries: add to ~/.ssh/config.d/<client>.conf

Host *
    AddKeysToAgent yes
    ServerAliveInterval 60
    ServerAliveCountMax 3

# Per-project/client overrides
Include ~/.ssh/config.d/*.conf
